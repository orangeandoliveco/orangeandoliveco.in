[project]
name = "orangeandoliveco-in"
version = "0.1.0"
description = "Generate the site oranngeoliveco.in"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jinja2>=3.1.6",
    "pydantic>=2.11.5",
    "requests>=2.32.3",
]
